
"use client";

// This file is intentionally left empty or as a placeholder
// as its previous functionality (displaying AI-derived coordinates)
// has been removed.
// It will be re-implemented or adapted later if a map feature
// based on API data (e.g., ACLED with geocoding) is developed.

export default function MapDisplay({ zones }: { zones: any[] }) {
  return (
    <div className="h-[300px] w-full flex flex-col items-center justify-center bg-muted/50 rounded-lg p-4 text-center">
      <p className="text-sm text-muted-foreground">
        Funcionalidade de mapa em desenvolvimento.
      </p>
    </div>
  );
}

    